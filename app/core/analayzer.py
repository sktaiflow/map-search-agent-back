def replace_query_with_synonym(query: str) -> str:
    """
    쿼리를 동의어로 변환
    프로세스:
        1. 쿼리를 키워드로 분리
        2. 분리된 키워드 -> C & C API 호출
        3. SCORE 기반 비교 후 -> 원본쿼리 변환
    """
    return query


async def replace_query_with_synonym(query: str) -> str:
    """
    쿼리를 동의어로 변환
    프로세스:
        1. 쿼리를 키워드로 분리
        2. 분리된 키워드 -> C & C API 호출
        3. SCORE 기반 비교 후 -> 원본쿼리 변환
    """
    return query
